import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign In Page - Sign in to access our deals !",
};
const SignIn = () => {
  <div>Sign In page</div>;
};
export default SignIn;
